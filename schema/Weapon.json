{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "Weapon.json",
  "title": "Weapon",
  "description": "Weapon definition.",

  "additionalProperties": false,

  "properties": {
    "name": {
      "type": "string",
      "minimum": 3,
      "maximum": 40
    },

    "range": {
      "$ref": "./enums/Range.json"
    },

    "dice": {
      "$ref": "./enums/AttackDice.json#/definitions/Pool"
    }
  },

  "required": ["name", "range", "dice"]
}
