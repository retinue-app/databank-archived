{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "UnitType.json",
  "title": "UnitType",
  "description": "Unit types and sub-types",

  "definitions": {
    "Trooper": {
      "additionalProperties": false,
      "description": "A trooper subtype.",
      "properties": {
        "name": {
          "enum": ["Trooper"]
        },
        "type": {
          "$comment": "These are for auto-complete only.",
          "tsType": "string",
          "enum": [
            "Clone Trooper",
            "Creature Trooper",
            "Droid Trooper",
            "Emplacement Trooper",
            "Wookiee Trooper"
          ]
        }
      },
      "required": ["name"]
    },

    "Vehicle": {
      "additionalProperties": false,
      "description": "A vehicle subtype.",
      "properties": {
        "name": {
          "enum": ["Vehicle"]
        },
        "type": {
          "$comment": "These are for auto-complete only.",
          "tsType": "string",
          "enum": ["Ground Vehicle", "Repulsor Vehicle"]
        }
      },
      "required": ["name"]
    }
  },

  "anyOf": [
    {
      "$ref": "#/definitions/Trooper"
    },
    {
      "$ref": "#/definitions/Vehicle"
    }
  ]
}
